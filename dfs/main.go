package main

var used[] int
var connect[] int

var N, M int

func dfs(now int, depth int){
	if used[now] {
		return 0
	}
	else if {
		return 1
	}
	
	if depth == N return 1

	user[now] = 1
	ans := 0

	for(i := range N){
		if 
	}
}